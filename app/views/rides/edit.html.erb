<header class="text-center">
  <h2>Edit ride for <%= current_user.email %> </h2>
</header>

<div class="container">
  <%= render 'form' %>
</div>

<div class='col-md-offset-4'>
  <%= link_to "Back", user_ride_path([current_user, @ride]), class: "btn btn-lg btn-success", id: "button-back" %>
</div>
